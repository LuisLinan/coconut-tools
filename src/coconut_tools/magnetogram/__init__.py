"""Magnetogram filtering and preparation tools.

The three root-level modules are the supported workflow entry points:
``sph_filtering``, ``NLD_implicit_method``, and ``Yaroslavsky_filter``.
Reusable implementations are organized by responsibility under ``core``,
``io``, ``processing``, ``filters``, and ``visualization``.  Historical code
kept only for reference lives under ``legacy``.
"""
